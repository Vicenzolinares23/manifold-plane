[package]
name = "mp-daemon"
description = "manifold-planed: the trajectory-based admission daemon."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "manifold-planed"
path = "src/main.rs"

[dependencies]
mp-core = { workspace = true }
mp-barrier = { workspace = true }
mp-adapters = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
